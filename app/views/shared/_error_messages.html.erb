<% if obj.errors.any? %>
  <div class="alert">
    <h2><%= obj.errors.count %>件のエラーがあります。</h2>
    <ul>
    <% obj.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>